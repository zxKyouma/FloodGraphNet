Historical best-submission checkpoints referenced by the experiment logs:

- `saved_models/baselines/model1_xgb_52344784_plus_endpoint_regime_v2_uphistema_minus_node_drop_edgeaware.pkl`
- `saved_models/baselines/model2_xgb_52344784_plus_endpoint_regime_v2_uphistema_minus_node_drop_edgeaware_plus_transform_cleanup_v3.pkl`

These checkpoint binaries are not present in the current repository snapshot, so they could not be copied here.

The matching public configs are available in:

- `configs/model1_best.yaml`
- `configs/model2_best.yaml`
